apiVersion: apps/v1
kind: Deployment
metadata:
  name: questions
spec:
  replicas: 1
  selector:
    matchLabels:
      app: questions
  template:
    metadata:
      labels:
        app: questions
    spec:
      containers:
        - name: questions
          image: dimitar171/solver-app_questions